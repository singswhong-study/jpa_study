<persistence xmlns="http://java.sun.com/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://java.sun.com/xml/ns/persistence http://java.sun.com/xml/ns/persistence/persistence_2_0.xsd"
             version="2.0">

    <persistence-unit name="jpa-study">
        <properties>
            <!-- H2 DB            -->
            <property name="javax.persistence.jdbc.driver" value="org.h2.Driver"/>
            <property name="javax.persistence.jdbc.user" value="sa"/>
            <property name="javax.persistence.jdbc.password" value="123"/>
            <property name="javax.persistence.jdbc.url" value="jdbc:h2:tcp://localhost/~/test"/>
            <property name="hibernate.dialect" value="org.hibernate.dialect.H2Dialect"/> <!-- 이걸로 dbms에 맞는 쿼리 설정. 다이얼랙트 -->
<!--            <property name="hibernate.hbm2ddl.auto" value="create"/>-->
            <!--
                스키마 자동생성 옵션 create, create-drop, update, validate, none 등
                customer 테이블을 드랍후, create 설정하고 jpa1 코드를 실행하게 되면
                Hibernate:
                    create table customer (
                       id varchar(255) not null,
                        name varchar(255),
                        regDate bigint not null,
                        primary key (id)
                    )
                DDL이 먼저 실행된다.
            -->


            <!-- Option           -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
            <property name="hibernate.use_sql_comments" value="true"/>
        </properties>
    </persistence-unit>
</persistence>